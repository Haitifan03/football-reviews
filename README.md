# Football Review Site

The purpose of this site is to provide a very simple blog for football reviews.

To run it:

```bash
npm install
npm run dev
```

alternatively, run the docker image.

This project will listen on port 3000